import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: "#808080",
      width: "100%"
    },
    back:{
      height: 200,
      backgroundColor: "#000",
    },
    title: {
      color: "#fff",
      fontSize: 20,
      alignItems: "center",
      margin: "auto",
    }
  });